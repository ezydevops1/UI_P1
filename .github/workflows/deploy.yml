name: CD
on:
  workflow_dispatch:
    inputs:
      environment:
        type: choice
        options: [dev, qa, prod]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo Deploying ${{ github.event.inputs.environment }}