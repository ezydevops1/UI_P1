name: CD
on:
  workflow_dispatch:
    inputs:
      environment:
        description: Target env
        required: true
        default: dev
        type: choice
        options: [dev, qa, prod]
  push:
    branches: [dev, qa, prod]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo '🚀 Deploy ran'
